// const icon = require("./icon")
/* const fs = require("fs")
const path = require("path")

let iconData = {}
fs.readFile(require.resolve("./icon.json"), async (err, data) => {
    if (err) return console.log(err);
    iconData = await JSON.parse(data)
})
console.log(require.resolve("./icon.json"))
console.log(iconData) */

module.exports = {
    // icon: require("../icon")
}